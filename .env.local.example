# 로컬 개발용 환경 변수
# Vercel 배포 시에는 다음과 같이 설정:
# NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
# NEXT_라는 접두사가 붙은 변수는 클라이언트와 서버 모두에서 접근 가능
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# 관리자 비밀번호 설정 (평문, 프로덕션에서는 ADMIN_PASSWORD_HASH 사용)
ADMIN_PASSWORD=

# JWT 비밀키 (프로덕션에서는 반드시 강력한 랜덤 키로 변경)
JWT_SECRET=

# 관리자 비밀번호 해시 (bcrypt, 프로덕션 환경에서 사용)
# ADMIN_PASSWORD_HASH=

# 보안 설정
NODE_ENV=development

# GitHub 파일 저장 사용 여부 (배포 환경에서만 true 권장)
USE_GITHUB_STORAGE=false

# GitHub 리포지토리 (owner/repo 형식, GitHub 저장 사용 시 필수)
GITHUB_REPOSITORY=your-username/your-repo

# GitHub 브랜치 (기본값: main)
GITHUB_BRANCH=master

# GitHub Personal Access Token (GitHub 저장 사용 시 필수)
GITHUB_TOKEN=ghp_your_token_here

# Git 사용자 정보 (커밋 시 사용)
GIT_USER_NAME="WallyLog Bot"
GIT_USER_EMAIL="bot@wallylog.com"

# SMTP 설정: 이메일 발송을 위한 SMTP 서버 정보
# 아래 내용을 깃허브 액션의 시크릿키로 등록필요
# GitHub 리포지토리 → Settings → Secrets and variables → Actions → New repository secret 에 등록하세요.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
# SMTP_FROM=notifications@your-domain.com
# SMTP_FROM_NAME="WallyLog"
# GITHUB_TOKEN=your_github_token_here

# OpenRouter API Key
OPENROUTER_API_KEY=sk-or-v1-your_api_key_here

# GitHub Actions 토큰 (API에 의한 영어 퀴즈 작성 스케줄러 실행에 필요)
# GitHub 리포지토리 → Settings → Secrets and variables → Actions → New repository secret 에 등록하세요.
DAILY_API_TOKEN=your_daily_api_token_here

# 리캡챠 키 (보안)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=